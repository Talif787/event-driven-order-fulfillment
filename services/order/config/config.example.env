# Order service configuration (Twelve-Factor: all config via environment).
# Copy to .env for local use. Do not commit real secrets.

SERVICE_NAME=order-service
ENVIRONMENT=development
HTTP_ADDR=:8080
LOG_LEVEL=info

# Database
DATABASE_URL=postgres://order:order@localhost:5432/order?sslmode=disable
DB_MAX_CONNS=20
DB_MIN_CONNS=2
DB_MAX_CONN_LIFETIME=1h

# Observability (leave OTLP endpoint empty to disable tracing locally)
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_TRACES_SAMPLER_RATIO=1.0

# Authentication (disabled by default for local development)
AUTH_ENABLED=false
AUTH_JWT_SECRET=
AUTH_JWT_ISSUER=order-service
AUTH_JWT_AUDIENCE=order-api

# HTTP server timeouts
HTTP_READ_HEADER_TIMEOUT=5s
HTTP_READ_TIMEOUT=15s
HTTP_WRITE_TIMEOUT=15s
HTTP_IDLE_TIMEOUT=60s
HTTP_SHUTDOWN_TIMEOUT=20s

# Kafka event backbone (comma separated brokers).
# In compose, services reach the broker at kafka:9092.
# From the host (for example a local go run or console consumer), use localhost:29092.
KAFKA_BROKERS=localhost:9092

# Outbox relay (polling publisher variant of the transactional outbox)
RELAY_POLL_INTERVAL=1s
RELAY_BATCH_SIZE=100

# Order projection consumer
PROJECTOR_GROUP_ID=order-projection
PROJECTOR_TOPICS=orders.events
