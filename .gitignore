# Binaries and build output
bin/
/out/
*.exe
*.test

# Environment and secrets
.env
*.local.env

# Go workspace sum (kept out of VCS; regenerate with go work sync)
go.work.sum

# Editor and OS
.idea/
.vscode/
.DS_Store

# Coverage
coverage.out
coverage.html

# Terraform working files
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Variable files that may hold secrets (keep the example)
*.tfvars
*.tfvars.json
!terraform.tfvars.example

# Plan output
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json